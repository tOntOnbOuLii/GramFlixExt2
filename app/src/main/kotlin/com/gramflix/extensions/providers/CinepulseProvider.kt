package com.gramflix.extensions.providers

import com.gramflix.extensions.config.RemoteConfig

class CinepulseProvider /*: MainAPI()*/ {
    private val key = "Cinepulse"
    // override var name = RemoteConfig.getProviderNameOrNull(key, "Cinepulse") ?: "Cinepulse"
    // override var mainUrl = RemoteConfig.getProviderBaseUrlOrNull(key, "https://cinepulse.cc") ?: "https://cinepulse.cc"
}

