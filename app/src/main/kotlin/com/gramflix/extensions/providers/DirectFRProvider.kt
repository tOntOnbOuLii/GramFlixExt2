package com.gramflix.extensions.providers

import com.gramflix.extensions.config.RemoteConfig

class DirectFRProvider /*: MainAPI()*/ {
    private val key = "DirectFR"
    // override var name = RemoteConfig.getProviderNameOrNull(key, "DirectFR") ?: "DirectFR"
    // override var mainUrl = RemoteConfig.getProviderBaseUrlOrNull(key, "https://directfr.lat") ?: "https://directfr.lat"
}

