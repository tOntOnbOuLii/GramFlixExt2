package com.gramflix.extensions.providers

import com.gramflix.extensions.config.RemoteConfig

class HDssProvider /*: MainAPI()*/ {
    private val key = "HDss"
    // override var name = RemoteConfig.getProviderNameOrNull(key, "HDss") ?: "HDss"
    // override var mainUrl = RemoteConfig.getProviderBaseUrlOrNull(key, "https://hdss.now") ?: "https://hdss.now"
}

