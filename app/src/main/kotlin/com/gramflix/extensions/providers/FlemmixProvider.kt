package com.gramflix.extensions.providers

import com.gramflix.extensions.config.RemoteConfig

class FlemmixProvider /*: MainAPI()*/ {
    private val key = "Flemmix"
    // override var name = RemoteConfig.getProviderNameOrNull(key, "Flemmix") ?: "Flemmix"
    // override var mainUrl = RemoteConfig.getProviderBaseUrlOrNull(key, "https://flemmix.club") ?: "https://flemmix.club"
}
