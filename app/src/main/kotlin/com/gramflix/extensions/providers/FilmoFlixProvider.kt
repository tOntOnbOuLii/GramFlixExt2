package com.gramflix.extensions.providers

import com.gramflix.extensions.config.RemoteConfig

class FilmoFlixProvider /*: MainAPI()*/ {
    private val key = "FilmoFlix"
    // override var name = RemoteConfig.getProviderNameOrNull(key, "FilmoFlix") ?: "FilmoFlix"
    // override var mainUrl = RemoteConfig.getProviderBaseUrlOrNull(key, "https://filmoflix.bike") ?: "https://filmoflix.bike"
}

