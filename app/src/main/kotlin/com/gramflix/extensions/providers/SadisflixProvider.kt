package com.gramflix.extensions.providers

import com.gramflix.extensions.config.RemoteConfig

class SadisflixProvider /*: MainAPI()*/ {
    private val key = "Sadisflix"
    // override var name = RemoteConfig.getProviderNameOrNull(key, "Sadisflix") ?: "Sadisflix"
    // override var mainUrl = RemoteConfig.getProviderBaseUrlOrNull(key, "https://sadisflix-officiel.com") ?: "https://sadisflix-officiel.com"
}

