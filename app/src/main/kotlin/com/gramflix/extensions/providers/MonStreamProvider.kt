package com.gramflix.extensions.providers

import com.gramflix.extensions.config.RemoteConfig

class MonStreamProvider /*: MainAPI()*/ {
    private val key = "MonStream"
    // override var name = RemoteConfig.getProviderNameOrNull(key, "MonStream") ?: "MonStream"
    // override var mainUrl = RemoteConfig.getProviderBaseUrlOrNull(key, "https://vww.monstream.cafe") ?: "https://vww.monstream.cafe"
}

