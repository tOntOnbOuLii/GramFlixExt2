package com.gramflix.extensions.providers

import com.gramflix.extensions.config.RemoteConfig

class FrenchStreamProvider /*: MainAPI()*/ {
    private val key = "FrenchStream"
    // override var name = RemoteConfig.getProviderNameOrNull(key, "French stream") ?: "French stream"
    // override var mainUrl = RemoteConfig.getProviderBaseUrlOrNull(key, "https://fs-miroir6.lol") ?: "https://fs-miroir6.lol"
}

