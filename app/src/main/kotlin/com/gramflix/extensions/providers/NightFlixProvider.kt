package com.gramflix.extensions.providers

import com.gramflix.extensions.config.RemoteConfig

class NightFlixProvider /*: MainAPI()*/ {
    private val key = "NightFlix"
    // override var name = RemoteConfig.getProviderNameOrNull(key, "NightFlix") ?: "NightFlix"
    // override var mainUrl = RemoteConfig.getProviderBaseUrlOrNull(key, "https://nightflix.world") ?: "https://nightflix.world"
}

