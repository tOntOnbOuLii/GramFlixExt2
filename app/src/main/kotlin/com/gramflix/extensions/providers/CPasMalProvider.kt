package com.gramflix.extensions.providers

import com.gramflix.extensions.config.RemoteConfig

class CPasMalProvider /*: MainAPI()*/ {
    private val key = "CPasMal"
    // override var name = RemoteConfig.getProviderNameOrNull(key, "CPasMal") ?: "CPasMal"
    // override var mainUrl = RemoteConfig.getProviderBaseUrlOrNull(key, "https://www.cpasmal.rip") ?: "https://www.cpasmal.rip"
}

