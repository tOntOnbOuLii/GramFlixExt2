package com.gramflix.extensions.providers

import com.gramflix.extensions.config.RemoteConfig

class PurstreamProvider /*: MainAPI()*/ {
    private val key = "Purstream"
    // override var name = RemoteConfig.getProviderNameOrNull(key, "Purstream") ?: "Purstream"
    // override var mainUrl = RemoteConfig.getProviderBaseUrlOrNull(key, "https://purstream.to") ?: "https://purstream.to"
}

