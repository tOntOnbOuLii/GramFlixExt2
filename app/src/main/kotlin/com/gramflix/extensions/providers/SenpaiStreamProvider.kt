package com.gramflix.extensions.providers

import com.gramflix.extensions.config.RemoteConfig

class SenpaiStreamProvider /*: MainAPI()*/ {
    private val key = "SenpaiStream"
    // override var name = RemoteConfig.getProviderNameOrNull(key, "Senpai-Stream") ?: "Senpai-Stream"
    // override var mainUrl = RemoteConfig.getProviderBaseUrlOrNull(key, "https://senpai.wiki") ?: "https://senpai.wiki"
}

