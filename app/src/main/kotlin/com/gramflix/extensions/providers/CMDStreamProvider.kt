package com.gramflix.extensions.providers

import com.gramflix.extensions.config.RemoteConfig

class CMDStreamProvider /*: MainAPI()*/ {
    private val key = "CMD-Stream"
    // override var name = RemoteConfig.getProviderNameOrNull(key, "CMD-Stream") ?: "CMD-Stream"
    // override var mainUrl = RemoteConfig.getProviderBaseUrlOrNull(key, "https://cmd-stream.xyz") ?: "https://cmd-stream.xyz"
}

